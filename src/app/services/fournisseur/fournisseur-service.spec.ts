import { FournisseurService } from './fournisseur-service';

describe('FournisseurService', () => {
  it('should create an instance', () => {
    expect(new FournisseurService()).toBeTruthy();
  });
});
